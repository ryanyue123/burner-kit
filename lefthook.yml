pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{js,ts,jsx,tsx}"
      run: pnpm oxlint {staged_files}

    - name: format
      glob: "*.{js,ts,jsx,tsx,json,css,html}"
      run: pnpm oxfmt --check {staged_files}

    - name: typecheck
      run: pnpm typecheck
